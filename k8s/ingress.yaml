apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fashion-ingress
  namespace: fashion
spec:
  rules:
    - host: fashion.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80